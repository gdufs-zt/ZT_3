<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.personal_health_system.dao.UserDao">  <!-- 确保这里的namespace与UserDao接口的全限定名一致 -->
    <select id="getUserByMassage" resultType="org.example.personal_health_system.bean.User">
        SELECT * FROM user WHERE username = #{username} AND password = #{password} AND state = 1
    </select>

    <select id="getAllUser" resultType="org.example.personal_health_system.bean.User">
        select * from user
        <if test="username != null ">
            where username like #{username}      <!--模糊查询-->
        </if>
        limit #{pageStart}, #{pageSize}     <!--分页-->
    </select>

        <select id="getUserCounts" resultType="java.lang.Integer">
            select count(*) from user
            <if test="username != null">
                where username like #{username}
            </if>
        </select>

    <update id="updateState">
        update user set state = #{state} where id = #{id}
    </update>

    <insert id="addUser">
        insert into user(username, password, email, role, state) values (#{username}, #{password}, #{email}, #{role}, #{state})
    </insert>

    <delete id="deleteUser">
        delete from user where id = #{id}
    </delete>

    <select id="getUpdateUser" resultType="org.example.personal_health_system.bean.User">
        select * from user where id = #{id}
    </select>

    <update id="editUser">
        update user set username = #{username}, password = #{password}, email = #{email} where id = #{id}
    </update>

</mapper>